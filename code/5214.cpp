#include <iostream>
#include <cstdlib>
#include <vector>
using namespace std;

int N, K, M;

int main() {
	cin >> N >> K >> M;

	vector<int> *v;
	v = (vector<int> *)malloc(sizeof(vector<int>) *M);

	for (int i = 0; i < M; i++) {
		for (int j = 0; j < K; j++) {
			
		}
	}


	return 0;
}